<h2>I am the index.html.erb file for lessons</h2>

<% if current_user.role == 'instructor'%>
  <%= link_to 'NEW Lesson', new_lesson_path %>
<% end %>

<% @lessons.each do |lesson| %>
  <p><%= link_to lesson.title, lesson_path(lesson) %></p>
<% end %>