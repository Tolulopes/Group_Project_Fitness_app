<h2>I am the index.html.erb file for categories</h2>

<%= link_to 'NEW Category', new_category_path %>

<% @categories.each do |category| %>
  <p><%= link_to category.name, category_path(category) %></p>
<% end %>